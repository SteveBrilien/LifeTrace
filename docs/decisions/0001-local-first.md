# ADR-0001：客户端采用本地优先架构

状态：Accepted

## 背景

LifeTrace 的核心场景包含弱网、无网、后台定位以及大图片上传。如果把服务器作为写入前置条件，用户会在最需要记录时遇到失败。

## 决策

- Room 本地数据库是客户端事实来源。
- 所有用户操作先提交本地事务。
- 云同步是异步派生流程。
- UI 显示同步状态，但不同步失败不阻止继续记录。

## 影响

优点：离线可靠、响应快、服务器故障不影响记录。

代价：需要同步队列、版本号、删除墓碑和冲突策略。
